* needed for alpha release
  * generate initialization of static fields
  * add a sanity test that passes

* The parser does not recognize syntax errors in certain places and copies them automatically

* write tests
  * class methods
  * static fields
  * class in class
  * preprocessor directives at the end of file
  * preprocessor directives in other weird places 
  * functions in functions
  * lamba expressions
  * initializations
  * automatically include header
  * test various combinations of command line arguments
* use assertions to check the validity of options instead of using complex conditional statements
* add license
* add antlr gradle plugin
